\begin{frame}
\frametitle{Roteiro da Apresentação}
\tableofcontents
\end{frame}

\section{Do Padrão ao Revisado}

\begin{frame}
\frametitle{O Mecanismo do Simplex Padrão (Tableau)}
Para entender a necessidade de evolução, precisamos olhar para o método clássico:
\vspace{0.3cm}
\begin{itemize}
    \item O \textbf{Tableau} mantém explicitamente a matriz $B^{-1}A$ inteira.
    \item A cada iteração, realizamos operações de linha (pivoteamento) em \textbf{todas as colunas} ($m \times n$).
    \item \textbf{A Ineficiência:} Calculamos e atualizamos colunas de variáveis que talvez \textbf{nunca} entrem na base.
    \item \textbf{O Resultado:} Muito esforço computacional desperdiçado e perda de esparsidade (fill-in).
\end{itemize}
\end{frame}

\begin{frame}
\frametitle{A Evolução: Simplex Revisado}
O Simplex Revisado não altera o caminho geométrico (visita os mesmos vértices), mas muda a álgebra:
\vspace{0.3cm}
\begin{block}{A Grande Diferença}
    Em vez de atualizar toda a matriz, trabalhamos com a matriz original $A$ e mantemos apenas a inversa da base $B^{-1}$ atualizada.
\end{block}
\vspace{0.2cm}
Isso transforma uma abordagem de "força bruta" (Padrão) em uma abordagem "cirúrgica" (Revisado).
\end{frame}

\begin{frame}
\frametitle{Comparativo: Padrão vs. Revisado}
\begin{table}[]
\centering
\small
\begin{tabular}{p{0.45\textwidth}|p{0.45\textwidth}}
\hline
\textbf{Simplex Padrão (Tableau)} & \textbf{Simplex Revisado} \\ \hline
Calcula $B^{-1}a_j$ para \textbf{todas} as colunas não-básicas. & \textbf{Computação sob Demanda:} Calcula dados apenas da variável candidata. \\ \hline
Custo por iteração: $\approx O(m \cdot n)$. & Custo por iteração: $\approx O(m^2)$ (independe de $n$). \\ \hline
Erros numéricos se propagam por todo o quadro. & Erros restritos à inversa $B^{-1}$ (pode ser resetada periodicamente). \\ \hline
\end{tabular}
\end{table}
\end{frame}

\section{O Algoritmo Revisado}

\begin{frame}
\frametitle{Passo a Passo Matemático}
\begin{block}{1. Multiplicadores Simplex (Vetor Dual)}
    Primeiro, calculamos o "preço sombra" das restrições:
    \[ \pi^T = c_B^T B^{-1} \]
\end{block}
\begin{block}{2. Pricing (Quem entra?)}
    Calculamos o custo reduzido ($\hat{c}$) para encontrar uma candidata:
    \[ \hat{c}_j = c_j - \pi^T a_j \]
    Se $\hat{c}_k > 0$, a variável $x_k$ entra na base.
\end{block}
\end{frame}

\begin{frame}
\frametitle{Algoritmo: Passos Finais}
\begin{block}{3. Coluna Atualizada (Apenas a vencedora)}
    Agora que escolhemos $x_k$, calculamos como ela afeta a base:
    \[ \alpha = B^{-1} a_k \]
\end{block}

\begin{block}{4. Teste da Razão (Quem sai?)}
    Idêntico ao padrão. Usamos a solução atual $\bar{b} = B^{-1}b$:
    \[ \theta = \min \left\{ \frac{\bar{b}_i}{\alpha_i} : \alpha_i > 0 \right\} \]
\end{block}

\begin{itemize}
    \item \textbf{Atualização:} A nova inversa $B^{-1}_{nova}$ é obtida multiplicando a anterior por uma matriz elementar (Eta).
\end{itemize}
\end{frame}

% --- EXEMPLOS (LIMPOS PARA LOUSA) ---
\section{Exemplo de Fixação}

\begin{frame}
\frametitle{Exemplo Resolvido (Demonstração)}
\textbf{Vamos resolver juntos no quadro!}

\vspace{0.5cm}
Maximizar $Z = 4x_1 + 3x_2$
\[ \text{Sujeito a: } \begin{cases} 2x_1 + x_2 \leq 10 \\ x_1 + x_2 \leq 8 \\ x_1, x_2 \geq 0 \end{cases} \]
\end{frame}

\section{Dinâmica de Sala}

\begin{frame}
\frametitle{Agora é com vocês!}
\textbf{Exercício proposto para a turma:}

\vspace{0.5cm}
Maximizar $Z = 3x_1 + 2x_2$
\[ \text{Sujeito a: } \begin{cases} x_1 + x_2 \leq 4 \\ 2x_1 + x_2 \leq 6 \\ x_1, x_2 \geq 0 \end{cases} \]
\end{frame}